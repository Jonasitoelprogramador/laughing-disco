import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    brand: {
      green: "#6dd47e",
      yellow: "#ffd55a",
      blue: "#303454",
    },
  },
});

export default theme;
